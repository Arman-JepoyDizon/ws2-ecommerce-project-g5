<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Email Verification - OnlyFreds</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" type="image/png" href="/img/OnlyFreds_logo.png"/>
  <style>
    .auth-card {
        border: none;
        border-radius: 1.5rem;
        box-shadow: 0 1rem 3rem rgba(0,0,0,0.1);
        overflow: hidden;
    }
    .icon-box {
        font-size: 3rem;
        margin-bottom: 1rem;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">

  <%- include("../partials/navbar") %>

  <div class="container py-5 flex-grow-1 d-flex align-items-center justify-content-center">
    <div class="col-12 col-md-6 col-lg-5">
        <div class="card auth-card text-center">
            <div class="card-body p-5">
                
                <div class="icon-box">
                    <% if (typeof message !== 'undefined' && message.includes('verified')) { %>
                        🎉
                    <% } else if (typeof message !== 'undefined' && (message.includes('Invalid') || message.includes('expired'))) { %>
                        ⚠️
                    <% } else { %>
                        ✉️
                    <% } %>
                </div>

                <h3 class="fw-bold mb-3">Account Status</h3>
                
                <p class="text-muted mb-4 fs-5">
                    <%= message %>
                </p>

                <div class="d-grid gap-2">
                    <a href="/auth/login" class="btn btn-primary rounded-pill fw-bold py-2">Go to Login</a>
                    <a href="/" class="btn btn-link text-decoration-none text-muted">Return Home</a>
                </div>

            </div>
        </div>
    </div>
  </div>

  <%- include("../partials/footer") %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>