<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password - OnlyFreds</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" type="image/png" href="/img/OnlyFreds_logo.png"/>
  <style>
    .auth-card {
        border: none;
        border-radius: 1.5rem;
        box-shadow: 0 1rem 3rem rgba(0,0,0,0.1);
        overflow: hidden;
    }
    .auth-header {
        background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
        color: white;
        padding: 2rem;
        text-align: center;
    }
    .form-control:focus {
        box-shadow: none;
        border-color: #6c757d;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">

  <%- include("../partials/navbar") %>

  <div class="container py-5 flex-grow-1 d-flex align-items-center justify-content-center">
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card auth-card">
            <div class="auth-header">
                <h3 class="fw-bold mb-0">Reset Password</h3>
                <p class="mb-0 opacity-75">Don't worry, it happens</p>
            </div>
            <div class="card-body p-4 p-md-5">

                <% if (error) { %>
                    <div class="alert alert-danger rounded-3"><%= error %></div>
                <% } %>
                <% if (message) { %>
                    <div class="alert alert-success rounded-3"><%= message %></div>
                <% } %>

                <form method="POST" action="/auth/forgot">
                    <div class="mb-4">
                        <label class="form-label small fw-bold text-uppercase text-muted">Email Address</label>
                        <input type="email" name="email" class="form-control bg-light border-0 py-2 rounded-3" placeholder="Enter your registered email" required>
                    </div>
            
                    <div class="cf-turnstile mb-3" data-sitekey="<%= process.env.TURNSTILE_SITEKEY %>"></div>
                    <button type="submit" class="btn btn-secondary w-100 py-2 rounded-pill fw-bold shadow-sm">Send Reset Link</button>
                </form>

                <div class="text-center mt-4">
                    <a href="/auth/login" class="text-decoration-none small text-muted">Back to Login</a>
                </div>
            </div>
        </div>
    </div>
  </div>

  <%- include("../partials/footer") %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
</body>
</html>