<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<title><%= title %></title>
<link rel="icon" type="image/png" href="/img/OnlyFreds_logo.png"/>
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="robots" content="noindex" />
<style>
:root { --fg:#111; --muted:#666; --accent:#2563eb; }
body { font-family: system-ui, Arial, sans-serif; color:var(--fg); display:flex;
min-height:100dvh; }
.wrap { margin:auto; max-width:640px; padding:2rem; text-align:center; }
h1 { font-size:2.25rem; margin:.25rem 0; }
p.lead { color:var(--muted); margin:.25rem 0 1rem; }
.links { display:flex; gap:.75rem; justify-content:center; flex-wrap:wrap;
margin-top:1rem; }
.links a { padding:.6rem .9rem; border:1px solid #ddd; border-radius:.5rem;
text-decoration:none; }
.links a.primary { border-color: var(--accent); color:#fff; background:
var(--accent); }
</style>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include("partials/navbar") %>
<main class="wrap" role="main" aria-labelledby="title">
<div aria-hidden="true" style="font-size:4rem">⚠️</div>
<h1 id="title">We hit a snag</h1>
<p class="lead">Please try again. If this keeps happening, return to the home
page.</p>
<div class="links">
<a class="primary" href="<%= (req && req.originalUrl) ? req.originalUrl : '/'
%>">Try again</a>
</div>
</main>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/script/navbar.js"></script>
</body>
</html>