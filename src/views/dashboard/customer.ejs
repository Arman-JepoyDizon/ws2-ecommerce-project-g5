<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Account - OnlyFreds</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .status-card {
        transition: all 0.2s;
        cursor: pointer;
        text-decoration: none;
        color: inherit;
    }
    .status-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 .5rem 1rem rgba(0,0,0,0.1) !important;
        color: #0d6efd;
    }
    .icon-badge {
        font-size: 2rem;
        margin-bottom: 0.5rem;
        display: block;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">
  <%- include("../partials/navbar") %>

  <div class="container py-5">
    
    <div class="d-flex align-items-center mb-5">
        <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center shadow" style="width: 80px; height: 80px; font-size: 2.5rem;">
            <%= user.name.charAt(0).toUpperCase() %>
        </div>
        <div class="ms-4">
            <h2 class="fw-bold mb-0"><%= user.name %></h2>
            <p class="text-muted mb-0"><%= user.email %></p>
            <a href="/user/profile" class="text-decoration-none small fw-bold">Edit Profile <i class="bi bi-pencil-square"></i></a>
        </div>
    </div>

    <% if (success) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= success %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
        <div class="card-header bg-white border-0 p-4 d-flex justify-content-between align-items-center">
            <h5 class="fw-bold mb-0">My Orders</h5>
            <a href="/user/orders" class="text-decoration-none small fw-bold">View Purchase History ></a>
        </div>
        <div class="card-body p-4 pt-0">
            <div class="row text-center g-3">
                <div class="col-6 col-md-2">
                    <a href="/user/orders#to_pay" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">💳</span>
                        <div class="small fw-bold">To Pay</div>
                        <% if (statusCounts.to_pay > 0) { %>
                            <span class="badge bg-danger rounded-pill mt-1"><%= statusCounts.to_pay %></span>
                        <% } %>
                    </a>
                </div>
                <div class="col-6 col-md-2">
                    <a href="/user/orders#to_ship" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">📦</span>
                        <div class="small fw-bold">To Ship</div>
                        <% if (statusCounts.to_ship > 0) { %>
                            <span class="badge bg-danger rounded-pill mt-1"><%= statusCounts.to_ship %></span>
                        <% } %>
                    </a>
                </div>
                <div class="col-6 col-md-2">
                    <a href="/user/orders#to_receive" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">🚚</span>
                        <div class="small fw-bold">To Receive</div>
                        <% if (statusCounts.to_receive > 0) { %>
                            <span class="badge bg-danger rounded-pill mt-1"><%= statusCounts.to_receive %></span>
                        <% } %>
                    </a>
                </div>
                <div class="col-6 col-md-2">
                    <a href="/user/orders#completed" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">⭐</span>
                        <div class="small fw-bold">Completed</div>
                    </a>
                </div>
                <div class="col-6 col-md-2">
                    <a href="/user/orders#cancelled" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">🚫</span>
                        <div class="small fw-bold">Cancelled</div>
                    </a>
                </div>
                <div class="col-6 col-md-2">
                    <a href="/user/orders#refund" class="status-card p-3 d-block rounded-3">
                        <span class="icon-badge">↩️</span>
                        <div class="small fw-bold">Return</div>
                    </a>
                </div>
            </div>
        </div>
    </div>

  </div>

  <%- include("../partials/footer") %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>