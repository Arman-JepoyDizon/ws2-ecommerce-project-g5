<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Categories - OnlyFreds</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/png" href="/img/OnlyFreds_logo.png"/>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">
  <%- include("../../partials/navbar") %>

  <div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h2 class="fw-bold text-dark mb-0">📂 Categories</h2>
        <p class="text-muted mb-0">Organize your Freds</p>
      </div>
      <a href="/dashboard/admin/categories/new" class="btn btn-primary rounded-pill px-4 shadow-sm">
        <span class="fw-bold">+ New Category</span>
      </a>
    </div>

    <% if (success) { %>
        <div class="alert alert-success rounded-4 shadow-sm border-0 d-flex align-items-center">
            <span class="me-2">✅</span> <%= success %>
        </div>
    <% } %>

    <div class="card border-0 shadow rounded-4 overflow-hidden">
      <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
            <thead class="bg-light border-bottom">
                <tr>
                <th class="ps-4 py-3 text-uppercase text-secondary small fw-bold">Name</th>
                <th class="py-3 text-uppercase text-secondary small fw-bold">Description</th>
                <th class="pe-4 py-3 text-end text-uppercase text-secondary small fw-bold">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (categories.length > 0) { %>
                    <% categories.forEach(cat => { %>
                    <tr>
                        <td class="ps-4 py-3">
                            <span class="fw-bold text-dark"><%= cat.name %></span>
                        </td>
                        <td class="py-3 text-muted"><%= cat.description %></td>
                        <td class="pe-4 py-3 text-end">
                        <a href="/dashboard/admin/categories/edit/<%= cat.categoryId %>" class="btn btn-sm btn-light text-primary rounded-pill fw-bold border me-1">Edit</a>
                        <form action="/dashboard/admin/categories/delete/<%= cat.categoryId %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this category?');">
                            <button class="btn btn-sm btn-light text-danger rounded-pill fw-bold border">Delete</button>
                        </form>
                        </td>
                    </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="3" class="text-center py-5 text-muted">
                            <div class="fs-1 mb-2">📭</div>
                            <p class="mb-0">No categories found.</p>
                        </td>
                    </tr>
                <% } %>
            </tbody>
            </table>
        </div>
      </div>
    </div>
  </div>

  <%- include("../../partials/footer") %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>