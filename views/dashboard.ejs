<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - <%= user.firstName %> <%= user.lastName %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Global Styles -->
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Include Navbar -->
  <%- include('partials/navbar', { activePage: 'dashboard' }) %>

  <div class="d-flex flex-grow-1 justify-content-center align-items-center">
    <div class="glass-card w-100" style="max-width: 600px;">
      <h1 class="mb-4">Welcome, <%= user.firstName %> <%= user.lastName %>!</h1>
      
      <ul class="list-group mb-4">
        <li class="list-group-item"><strong>User ID:</strong> <%= user.userId %></li>
        <li class="list-group-item"><strong>Email:</strong> <%= user.email %></li>
        <li class="list-group-item"><strong>Role:</strong> <%= user.role %></li>
        <li class="list-group-item"><strong>Status:</strong> Active</li>
      </ul>

      <div class="d-grid gap-2">
        <a href="/users/edit/<%= user._id %>" class="btn btn-custom">Edit Profile</a>
        
        <% if (user.role === 'admin') { %>
          <a href="/users/admin" class="btn btn-warning">Admin Dashboard</a>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
